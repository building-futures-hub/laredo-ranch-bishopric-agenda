import React from "react";
import ReactDOM from "react-dom/client";
import BishopricAgenda from "./BishopricAgenda";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <BishopricAgenda />
  </React.StrictMode>
);
